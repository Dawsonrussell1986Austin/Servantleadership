import http from 'node:http';import { readFile } from 'node:fs/promises';import { existsSync } from 'node:fs';import path from 'node:path';
const ROOT=path.resolve('dist');const T={'.html':'text/html','.js':'text/javascript','.css':'text/css','.json':'application/json','.png':'image/png','.jpg':'image/jpeg','.jpeg':'image/jpeg','.svg':'image/svg+xml','.ico':'image/x-icon','.woff':'font/woff','.woff2':'font/woff2','.ttf':'font/ttf','.mp3':'audio/mpeg','.map':'application/json'};
function R(u){let p=decodeURIComponent(u.split('?')[0]);if(p==='/')return path.join(ROOT,'index.html');if(/^\/liturgy\/[^/]+$/.test(p))return path.join(ROOT,'liturgy','[id].html');if(/^\/category\/[^/]+$/.test(p))return path.join(ROOT,'category','[id].html');const d=path.join(ROOT,p);if(existsSync(d))return d;const h=path.join(ROOT,p+'.html');if(existsSync(h))return h;return path.join(ROOT,'index.html');}
http.createServer(async(rq,rs)=>{try{const f=R(rq.url);const b=await readFile(f);rs.writeHead(200,{'content-type':T[path.extname(f)]||'application/octet-stream'});rs.end(b);}catch{rs.writeHead(404);rs.end('nf');}}).listen(8099,()=>{});
